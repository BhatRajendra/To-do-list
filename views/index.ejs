<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>to do </title>
    <link rel="stylesheet" href="/styles.css">
</head>
<body>
    <div class="container">
            <h1>My To-do list</h1>

            <h3><%= date %></h3>
            <% for( let i = 0; i < todo.length; i++ ) { %>
                <form action="/done/<%= todo[i].id %>" method="get">
                    <h2><%= todo[i].content %></h2>
                    <button>done</button>
                </form>
            <% } %>
    
        

            <form action="/new" method="post">
                <textarea name="content" placeholder="   what to do?🤔" maxlength="40" rows="1"></textarea>
                <button class="add">add</button>
            </form>
        
    </div>
</body>
</html>